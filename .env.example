# ===========================================
# Medical Appointment Scheduling Agent
# Environment Configuration
# ===========================================

# -------------------------------------------
# LLM Configuration (Required)
# -------------------------------------------
# Provider: "openai" or "anthropic"
LLM_PROVIDER=openai

# Model to use
# OpenAI: gpt-4-turbo, gpt-4, gpt-3.5-turbo
# Anthropic: claude-3-opus-20240229, claude-3-sonnet-20240229
LLM_MODEL=gpt-4-turbo

# OpenAI API Key (required if using OpenAI)
OPENAI_API_KEY=your-openai-api-key-here

# Anthropic API Key (required if using Anthropic)
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# -------------------------------------------
# Server Configuration
# -------------------------------------------
BACKEND_PORT=8000

# -------------------------------------------
# Calendly API Configuration (Optional)
# -------------------------------------------
# If not configured, the system will use mock/local scheduling.
# When configured, it will use real Calendly API with mock as fallback.

# Calendly Personal Access Token (PAT)
# Get yours at: https://calendly.com/integrations/api_webhooks
# Click "Get a token now" under Personal Access Tokens
CALENDLY_API_KEY=

# Optional: Pre-configured URIs (auto-detected if not set)
# These are automatically fetched from the API if you don't provide them
CALENDLY_ORGANIZATION_URI=
CALENDLY_USER_URI=
CALENDLY_EVENT_TYPE_URI=

# -------------------------------------------
# How to get Calendly API Key:
# -------------------------------------------
# 1. Log in to your Calendly account
# 2. Go to: https://calendly.com/integrations/api_webhooks
# 3. Under "Personal Access Tokens", click "Get a token now"
# 4. Name your token (e.g., "Medical Scheduler")
# 5. Click "Create Token"
# 6. Copy the token and paste it above as CALENDLY_API_KEY
#
# Note: Keep this token secure! It grants access to your Calendly data.
# -------------------------------------------

# -------------------------------------------
# Calendly Event Type Mapping (Optional)
# -------------------------------------------
# The system will automatically map your local appointment types
# (consultation, followup, physical, specialist) to Calendly event types
# based on name matching. If no match is found, it uses the first event type.
#
# For custom mapping, create event types in Calendly with these names:
# - "Consultation" or "General Consultation"
# - "Follow-up" or "Follow Up"
# - "Physical Exam" or "Physical"
# - "Specialist Consultation" or "Specialist"
# -------------------------------------------
